[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "arf"
dynamic = ["version"]
description = "Advanced Recording Format for acoustic, behavioral, and physiological data"
readme = "README.rst"
requires-python = ">=3.11"
authors = [
    {name = "Dan Meliza", email = "dan@meliza.org"},
]
maintainers = [
    {name = "Dan Meliza", email = "dan@meliza.org"},
]
license = "BSD-3-Clause"
license-files = ["COPYING"]
keywords = ["neuroscience", "data format"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: C++",
    "Topic :: Scientific/Engineering",
    "Operating System :: Unix",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Natural Language :: English",
]
# Floors are the oldest release that ships a wheel for the interpreter, on
# every platform tested: manylinux and musllinux (the dev sandbox is Alpine),
# macOS arm64, and win_amd64. Below these, pip and uv fall back to building
# from source, which for numpy and h5py means a compiler, HDF5 headers, and
# several minutes per job. Markers are mutually exclusive so only one applies.
dependencies = [
    "h5py>=3.15.0",
    "numpy>=1.26.2; python_version < '3.13'",
    "numpy>=2.1.0; python_version == '3.13'",
    "numpy>=2.3.2; python_version >= '3.14'",
    "packaging>=24.0",
]

[project.urls]
Homepage = "https://github.com/melizalab/arf"

[dependency-groups]
dev = [
    "pytest>=5.0",
    "pytest-cov >= 4.1.0",
    "ruff>=0.7.0",
    "ty>=0.0.69",
]

[tool.hatch.version]
path = "arf.py"

[tool.pytest.ini_options]
python_files = ["test_*.py", "*_test.py"]
addopts = "-v --cov=arf --cov-report=term-missing"
testpaths = ["tests"]

[tool.black]
line-length = 88
target-version = ["py37"]
include = '\.pyi?$'

[tool.ruff]
line-length = 88
target-version = "py37"
extend-exclude = ["build", "attic"]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
]
ignore = ["E221", "E501", "E701"]

[tool.ty.src]
include = ["arf.py"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
strict_optional = true
check_untyped_defs = true

[tool.hatch.build]
include = ["arf.py"]
exclude = ["*test*"]
artifacts = ["/README.rst"]
